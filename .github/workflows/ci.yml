name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

env:
  OPENBLAS_NUM_THREADS: 1
  OMP_NUM_THREADS: 1

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.11"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libopenblas-dev

      - name: Install zkml-system
        run: pip install -e ".[dev]"

      - name: Install tda_fingerprint (sibling dependency)
        run: |
          pip install git+https://github.com/dweyhe/tda_fingerprint.git

      - name: Build Rust backend (optional, best-effort)
        continue-on-error: true
        run: |
          pip install maturin
          cd rust_backend && maturin develop --release
        
      - name: Run tests
        run: |
          python -m pytest tests/ -v --tb=short -x
        env:
          PYTHONPATH: ${{ github.workspace }}

      - name: Run demo
        run: python demo.py
        env:
          PYTHONPATH: ${{ github.workspace }}
